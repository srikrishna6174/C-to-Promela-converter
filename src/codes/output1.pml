/* Automatically translated from C to Promela */
/* Original file: input.c */

proctype (chan in ; int x; int y) {
    chan ret_ = [0] of { int };
    int tmp;
    if(y==0) return x;
        :: else ->
    end:
        goto end;
}
}
